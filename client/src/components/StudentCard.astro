---
import type { Group, Student } from "@/lib/database";
import StudentImage from "./StudentImage.astro";

interface Props {
  student: Student;
  group?: Group;
  class?: string;
  hideName?: boolean;
  hideGroup?: boolean;
}
const { student, group, class: cardClass, hideName, hideGroup } = Astro.props;
---

<a
  href={`/students/${student.id}`}
  class={"card shadow bg-base-100 flex flex-col h-full card-xs sm:card-md " +
    (cardClass || "")}
>
  <figure class="shrink-0">
    <StudentImage src={student.image} gender={student.gender} />
  </figure>

  {
    !hideName || (!hideGroup && group) ? (
      <div class={"card-body " + (hideGroup && "items-center")}>
        {!hideName && (
          <h2 class="card-title text-base sm:text-lg">
            {student.name} {student.lastName}
          </h2>
        )}
        {group && !hideGroup && (
          <p class="text-sm opacity-70">
            Clase: {group.year}
            {group.letter}
          </p>
        )}
      </div>
    ) : null
  }
</a>
